name: Update profile stats

on:
  schedule:
    # every hour, offset from :00 to reduce Actions congestion
    - cron: "17 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/generate_stats.py"
      - "scripts/generate_projects.py"
      - ".github/workflows/stats.yml"

concurrency:
  group: profile-stats
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate stats / languages / contributions SVGs
        env:
          GITHUB_USERNAME: canekin
          # Prefer a classic/fine-grained PAT with `repo` + read:user
          # (private repo counts + private contribution calendar).
          PROFILE_TOKEN: ${{ secrets.PROFILE_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/generate_stats.py

      - name: Generate recently-pushed projects SVG + README list
        env:
          GITHUB_USERNAME: canekin
          PROFILE_TOKEN: ${{ secrets.PROFILE_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/generate_projects.py

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add generated/stats.svg generated/languages.svg generated/contributions.svg generated/projects.svg README.md
          if git diff --staged --quiet; then
            echo "No profile asset changes"
            exit 0
          fi
          git commit -m "chore: refresh live profile stats & projects"
          git pull --rebase origin main
          git push
